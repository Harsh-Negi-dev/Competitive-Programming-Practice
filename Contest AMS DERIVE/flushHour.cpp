#include<bits/stdc++.h>
using namespace std;
int main() {
    int n, k, f;
    cin >> n >> k >> f;
    vector<pair<int, int>> job(n);
    while (n--) {
        int t, d;
        cin >> t >> d;

    }
    return 0;
}