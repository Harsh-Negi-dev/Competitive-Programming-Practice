#include<bits/stdc++.h>
using namespace std;
int main() {
    int q;
    cin >> q;
    while (q--) {
        vector<int> arr;
        for (int i = 0; i < q; i++) {
            cin >> arr[i];
        }
    }
    return 0;
}